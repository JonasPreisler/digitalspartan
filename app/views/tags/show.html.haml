.span
  %h3
    = @tag.name
#posts
  - if @post
    - @post.each do |post|
      .post
        .post_image
          = link_to (image_tag post.image.url(:small)), post
        .post_content
          .title
            %h2= link_to post.title, post
          .data.clearfix
            %p.username
              Shared by
              = link_to (post.user.name), user_path(post.user.name)



            %p.buttons
              %span
                %i.fa.fa-comments-o
                = post.comments.count
              %span
                %i.fa.fa-thumbs-o-up
                = post.get_likes.size
  - else
  %br
  %br
  Sorry mate... nothing to see here (yet).
  %br
  %br
  %br
  %br
  = link_to "Back to tags", tags_path, class: "button-header"